import Popover from "react-bootstrap/Popover";

const PopoverSummary = () => {
  return (
    <Popover id="popover-basic">
      <Popover.Body>No ice cream will actually be delivered</Popover.Body>
    </Popover>
  );
};

export default PopoverSummary;
